{% extends 'base.html.twig' %}

{% block body %}

    {% if title is defined %}
    <h1 class="page__title">{{title}}</h1>
    {% else %}
    <h1>Header</h1>
    {% endif %}

    {{form(form)}}
    
{% endblock %}