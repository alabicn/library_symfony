{% extends 'base.html.twig' %}

{% block title %}
{{title}}
{% endblock %}

{% block body %}
    {% if title is defined %}
        <h1 class="page__title">{{title}}</h1>
    {% else %}
        <h1>Header</h1>
    {% endif %}

    <div id="settings">
        <div id="member__photo-details">
            <img id="member-img" src="/assets/img/{{ app.user.srcPhoto }}" alt="{{ app.user.altPhoto }}" title="{{ app.user.titlePhoto }}"/>
            <div>
                {{ form(form, {'attr': {'id': 'photo_upload-form'}}) }}
            </div>
            <p class="reset_password">
                <a href={{ path('password_update') }}>Reset Password?</a>
            </p>
        </div>
        <div id="member">
            <div id="member_name">
                <h2><span>{{app.user.uname}}</span> <span>Member since : {{ app.user.registrationDate | date("F Y") }}</span></h2>
            </div>
            <div id="member_info">
                {{app.user.uname}}
                {{app.user.gender}}
                {{app.user.birthdate | age}}
            </div>
        </div>
    </div>

{% endblock %}
